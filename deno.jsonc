{
  "tasks": {
    "build":"deno run --allow-all getArticles.ts && deno task dev",
    "dev": "deno run --allow-all --watch main.ts --dev",
    "serve": "deno run --allow-net --allow-read --no-check main.ts",
  }
}
